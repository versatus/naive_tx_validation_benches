// Validator processing unit is a scalable struct that can 
// validate transactions

pub struct Validator;

impl Validator {
    pub fn start() {}
    pub fn validate(tx: String) -> bool {
        true
    }
    
}